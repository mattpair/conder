{
  "name": "conder",
  "version": "0.0.1",
  "description": "conder CLI",
  "private": true,
  "types": "build/types/types.d.ts",
  "bin": {
    "conder": "bin/conder"
  },
  "scripts": {
    "clean-build": "rm -rf ./build",
    "compile": "tsc -p .",
    "build": "yarn clean-build && yarn compile",
    "prepublishOnly": "yarn build",
    "pretest": "npm run build",
    "test": "jest"
  },
  "files": [
    "tsconfig.json",
    "build",
    "bin"
  ],
  "dependencies": {
    "conduit_compiler": "file:../../conduit_src/conduit_compiler",
    "gluegun": "latest"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/mongodb": "^3.5.31",
    "@types/node": "^12.7.11",
    "jest": "^24.1.0",
    "mongodb": "^3.6.2",
    "prettier": "^1.12.1",
    "ts-jest": "^24.1.0",
    "ts-node": "^8.4.1",
    "typescript": "^3.6.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/build/"
    ]
  }
}
