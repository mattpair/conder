{
  "name": "conder_core",
  "licenses": [
    {
      "license": "NCSA"
    }
  ],
  "scripts": {
    "gen": "node -e 'require(\"./dist/src/main/ops/index.js\").writeMain()'",
    "precompile": "npm run gen && cd src/main/ops/rust && cargo build",
    "compile": "tsc -b",
    "pretest": "npm run compile",
    "test": "jest --runInBand"
  },
  "author": "Jeremy",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.8",
    "@types/mongodb": "^3.5.27",
    "@types/node": "^14.10.1",
    "jest": "^26.2.2",
    "prettier": "^2.0.5",
    "ts-jest": "^26.1.4",
    "typescript": "^3.9.3"
  },
  "dependencies": {
    "isomorphic-fetch": "^2.2.1",
    "mongodb": "^3.6.2"
  },
  "jest": {
    "preset": "ts-jest",
    "testMatch": [
      "**/?(*.)+(spec).[tj]s"
    ],
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/dist/"
    ]
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "version": "0.0.6"
}
